{
  "description": "Optional Claude adapter: reminds a new session to read an existing SHIP.md once per session and stays silent otherwise.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/ledger-check.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
