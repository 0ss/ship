The previous agent says invite rate limiting is done. It must allow at most 5 invite creations per user per 60 seconds. `InviteService.create` must accept a `user_id` keyword argument and enforce the limiter; its constructor must accept a `limiter` keyword argument for injection. A blocked creation must raise `RateLimited` without adding an invite. Different users have independent quotas, and the first user can create again after 60 seconds. The existing tests only cover a single user's `RateLimiter.allow` happy path. Verify this end to end, fix the implementation and tests if wrong, run them, and only claim completion after the behavior works.
