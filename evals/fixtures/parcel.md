The parcel map has visible parcel buttons, but the actual request is: when I click a rendered parcel, its details should open. A previous agent called this done because `/map.html?fixture=P-101` displayed details; that never clicked a parcel. Fix the real click journey, preserve the fixture route, and verify it in the running page using the normal `/map.html` route with a click on a rendered parcel. If the environment prevents a browser check, leave the ask implemented or blocked in your state, not verified.
