# Requirements

| # | requirement | source | covers | state |
|---|---|---|---|---|
| R1 | screener hard-stops any deal that fails the screen before any diligence spend — no override | sync-01 Sara "it should stop. Before we spend anything" → Sara reversal "hard stop, no override, because the override will just get clicked every time" | - | open |
| R2 | deals page table sortable by date and by amount | sync-01 Dan "you can't sort it. Can't sort by date, can't sort by amount" | - | open |
| R3 | deals page general visual redesign | sync-01 Dan "the whole thing needs to look better but I don't know what I mean by that yet" / Sara "park it" | - | deferred |
| R4 | CSV export of the whole filtered deal set (not just visible rows), delivered by emailing the file rather than a browser download | sync-01 Sara "the export... everything, the whole filtered set" + "it should email them the file rather than downloading, because half of them are on the iPad" | - | open |
| R5 | screener rejections record the reason to a log readable later | sync-01 Sara "when the screener rejects something there's no record of why. That needs to be written down somewhere we can read later" | - | open |
| R6 | caching work | sync-01 Sara "not this week" | - | deferred |

## superseded
- R1 "stop before any spend" → "warn first, then stop, people will want the override" → "hard stop, no override" (end of sync-01, Sara "actually hold on. Go back.")

## not asked for
- (nothing)
