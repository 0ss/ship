# Requirements

| # | requirement | source | covers | state |
|---|---|---|---|---|
| R1 | screener is a hard stop: a deal that fails the screen spends nothing on diligence, no override | fixture-01 transcript, Sara "it should stop. Before we spend anything" → Sara "hard stop, no override" | - | open |
| R2 | deals page table sortable by column, at least date and amount | fixture-01 transcript, Dan "you can't sort it. Can't sort by date, can't sort by amount" | - | open |
| R3 | deals page "needs to look better" — no definition given, parked by Sara | fixture-01 transcript, Dan "the whole thing needs to look better but I don't know what I mean by that yet" / Sara "park it" | - | deferred |
| R4 | CSV export on the deals page covering the whole filtered set (not just visible rows), delivered by emailing the file to the user instead of a browser download | fixture-01 transcript, Sara "someone asked for CSV export off that same page" + "everything, the whole filtered set" + "it should email them the file rather than downloading, because half of them are on the iPad" | - | open |
| R5 | audit log: when the screener rejects a deal, record why, readable later | fixture-01 transcript, Sara "there's no record of why. That needs to be written down somewhere we can read later" | - | open |
| R6 | caching work — explicitly not this week | fixture-01 transcript, Sara "we should look at that caching thing eventually but that's not this week" | - | deferred |

## superseded
- R1 "stop before we spend anything" → "warn first, then stop" (Dan's override suggestion, accepted) → "hard stop, no override, because the override will just get clicked every time" (Sara, later in same meeting). Latest wins: no override path.

## not asked for
- (nothing)

## ignored
- (nothing; no instruction-shaped text in the material)
