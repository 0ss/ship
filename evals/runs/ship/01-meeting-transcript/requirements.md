# Requirements

| # | requirement | source | covers | state |
|---|---|---|---|---|
| R1 | screener hard-stops a failing deal before any diligence spend — no override | fixture-01 transcript, Sara "it should stop. Before we spend anything" → Sara "hard stop, no override" | — | open |
| R2 | deals page table sortable by column (date, amount, others) | fixture-01, Dan "you can't sort it. Can't sort by date, can't sort by amount" | — | open |
| R3 | deals page visual redesign — "needs to look better", speaker can't say what yet | fixture-01, Dan "the whole thing needs to look better but I don't know what I mean by that yet" | — | deferred |
| R4 | CSV export of the whole filtered set off the deals page, delivered by email rather than browser download | fixture-01, Sara "someone asked for CSV export" + "everything, the whole filtered set" + "email them the file rather than downloading, because half of them are on the iPad" | — | open |
| R5 | audit log records why the screener rejected a deal, readable later | fixture-01, Sara "there's no record of why. That needs to be written down somewhere we can read later" | — | open |
| R6 | caching | fixture-01, Sara "we should look at that caching thing eventually but that's not this week" | — | deferred |

## superseded
- R1 "stop before we spend anything" → "warn first, then stop, people will want the override" → "forget the warning, hard stop, no override" (Sara, end of meeting)

## not asked for
- _(nothing yet)_
